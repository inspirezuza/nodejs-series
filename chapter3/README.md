```console
npm i nodemon -g // npm install nodemon -global
npm init -y // create package.json
npm i date-fns // install date-fns module

npm install // use when download other repo

npm i nodemon -D //save nodemon as a dev dependency
npm i nodemon --save-dev //same as above

npm uninstall nodemon -D //remove nodemon in dev dependency
```